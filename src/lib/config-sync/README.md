/**
 * Vendored from flagmint-js-sdk/sdk/core/config-sync (no FlagClient).
 *
 * Keep this folder in sync when the SDK config-sync modules change:
 *   npm run sync:config-sync
 *
 * Docker / standalone builds only see this repo, so we cannot rely on
 * a sibling path like ../flagmint-js-sdk.
 */
